# Security: Disable directory listing
Options -Indexes

# Security: Block direct access to PHP files if needed (Optional, usually APIs allow direct access)
# <Files "*.php">
#     Order deny,allow
#     Deny from all
#     Allow from 127.0.0.1
# </Files>
